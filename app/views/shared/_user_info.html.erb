<%= link_to gravatar_for(current_user,
  size: Settings.gravatar.size_for_user), current_user %>
<h1><%= current_user.name %></h1>
<span><%= link_to t(".user_link"), current_user %></span>
<span><%= pluralize current_user.microposts.size, t(".micropost") %></span>
